<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tecsocapp">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:gravity="center"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".PerfilEmpresaView"></activity>
        <activity android:name=".pesquisa.PesquisaMainActivity" />
        <activity android:name=".CadastroPesquisa" />
        <activity android:name=".pesquisaempresa.PesquisasEmpresa" />
        <activity android:name=".EmpresaMain" />
        <activity android:name=".CadastroEmpresa" />
        <activity android:name=".requisito_empresa" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-sdk tools:overrideLibrary="com.firebase.ui.database,com.firebase.ui.storage,com.firebase.ui.common" />

</manifest>